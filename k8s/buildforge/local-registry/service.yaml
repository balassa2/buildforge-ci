apiVersion: v1
kind: Service
metadata:
  name: local-registry
  namespace: buildforge
  labels:
    app: local-registry
spec:
  selector:
    app: local-registry
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
  type: ClusterIP
